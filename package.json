{
  "name": "akasha-library",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config vite.config.js",
    "dev:script-editor": "vite --config vite.config.script-editor.js",
    "build": "vite build --config vite.config.js && vite build --config vite.config.script-editor.js && node scripts/sync-sw.js",
    "build:spreadsheet": "vite build --config vite.config.js",
    "build:script-editor": "vite build --config vite.config.script-editor.js",
    "build:public": "npm run build && node scripts/build.js --mode=public",
    "build:private": "npm run build && node scripts/build.js --mode=private",
    "preview": "vite preview --config vite.config.js",
    "test:regression": "node --test tests/regressions.test.mjs",
    "test": "npm run test:regression && npm run build && node scripts/sync-sw.js --check && node scripts/check-msg-types.js && node -e \"const fs=require('fs'); const checks=['dist/spreadsheet/index.html','dist/script-editor/index.html','index.html','core/export/bridge.js','core/export/toPayload.js','core/export/fromPayload.js','modules/markdown/index.html','modules/pdf-reader/index.html','public-library/index.html']; let ok=true; checks.forEach(f=>{if(!fs.existsSync(f)){console.error('MISSING: '+f);ok=false;}}); if(ok)console.log('✓ All checks passed');else process.exit(1);\""
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "vite": "^8.0.16"
  }
}
