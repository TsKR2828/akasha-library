# 阿卡夏圖書館 — UI/UX 設計指令書

## 專案概述

**產品名稱：** 阿卡夏圖書館（Akasha Library）
**性質：** 通用雲端書庫 PWA — 結合 MD 編輯、PDF 閱讀、Excel 試算表、書籍排版器
**目標用戶：** 喜歡閱讀與寫作的個人用戶、小型創作團隊
**品牌調性：** 古典學院風 × 現代極簡 — 像在一座深夜的私人圖書館裡工作

---

## 品牌核心

| 屬性 | 描述 |
|------|------|
| 風格 | 深色系、沉穩、書卷氣、有溫度但不花俏 |
| 氣質 | 古典圖書館 + 現代工具 — 像在黃銅燈下用最新科技 |
| 英文字體感覺 | 古典襯線（目前用 Cormorant Garamond） |
| 中文字體感覺 | 正統明體（目前用 Noto Serif TC） |
| 禁忌 | 不要圓角泡泡風、不要亮色系、不要科技感太重、不要遊戲感 |

---

## 色彩系統（目前使用中，可做為基底調整）

```css
--navy: #1E2430        /* 主背景 — 深海軍藍 */
--navy-light: #2A3240  /* 卡片/面板背景 */
--navy-hover: #343E4E  /* 邊框/hover 態 */
--gold: #C9A86A        /* 主強調色 — 燙金感 */
--gold-dim: #A08550    /* 次強調 */
--gold-glow: rgba(201,168,106,0.15)  /* 光暈/選中態背景 */
--cream: #F4F0E6       /* 主文字色 — 書頁米色 */
--text-primary: #F4F0E6
--text-secondary: #9BA3B0  /* 輔助文字 — 銀灰 */
--danger: #C75B5B      /* 刪除/錯誤 */
--success: #6AAE7F     /* 成功/已同步 */
```

**設計師可以：**
- 微調色值，但保持「深底 + 金色強調 + 米色文字」的大方向
- 加入漸層或紋理（例如紙張質感疊加）
- 加入深色/淺色模式切換（淺色模式 = 反轉：米色底 + 深色文字 + 金色強調）

---

## 全域佈局架構

### App Shell（主畫面）

```
┌──────────────────────────────────────────────────┐
│ [SIDEBAR 260px]  │         [MAIN AREA]           │
│                  │  ┌─────────────────────────┐  │
│ Logo/Brand       │  │ Topbar (標題 + 動作鈕)   │  │
│ ─── Auth ───     │  ├─────────────────────────┤  │
│ 用戶頭像 / 登入  │  │                         │  │
│ 同步狀態         │  │  Content                │  │
│ ─── Nav ───      │  │  (Dashboard / iframe)   │  │
│ ☰ 總覽           │  │                         │  │
│ 📚 個人書庫      │  │                         │  │
│ 🌎 公共書庫      │  │                         │  │
│ ✎ Markdown       │  │                         │  │
│ 📖 PDF 閱讀器    │  │                         │  │
│ 📊 試算表        │  │                         │  │
│ 💛 書籍排版器    │  │                         │  │
│ ⚙ AI 設定        │  │                         │  │
│ ─── Footer ───   │  │                         │  │
│ v0.1 · 隱私政策  │  └─────────────────────────┘  │
└──────────────────────────────────────────────────┘
```

**響應式：**
- 桌面 (>768px)：Sidebar 常駐
- 平板/手機 (≤768px)：Sidebar 可收合成漢堡選單

### Dashboard 內容

- 4 張模組卡片（Markdown / PDF / 試算表 / 書籍排版器）
- 每張卡有：圖示、標題、描述、支援格式標籤
- 下方：最近開啟的檔案列表（檔名、類型、開啟時間）

---

## 需要設計的頁面/元件清單

### 1. App Shell（index.html）

| 元件 | 說明 | 互動狀態 |
|------|------|----------|
| Sidebar Brand | 品牌 Logo + 英文名 "AKASHA LIBRARY" | — |
| Auth 區塊 | 未登入：Google 登入按鈕 / 已登入：頭像 + 名字 + 登出 | hover, loading |
| Sync 狀態 | 小字顯示「已同步 / 同步中 / 離線模式 / 同步失敗」 | 四種狀態 |
| Nav 項目 | 圖示 + 標題 + 副標題 | default, hover, active |
| Module Cards | 儀表板上的四張入口卡片 | default, hover |
| Recent Files 列表 | 檔名、圖示、時間、操作 | hover, empty state |
| Topbar | 頁面標題 + 右側動作按鈕（匯入/新建） | — |

### 2. PDF 閱讀器（modules/pdf-reader/）

```
┌─────────────────────────────────────────────────────────────┐
│ Topbar: [返回] [檔名] [AI圖書館員按鈕] [頁碼] [zoom]        │
├────────────┬──────────────────────────────┬─────────────────┤
│ Left       │                              │ Right           │
│ Sidebar    │     PDF Canvas               │ AI Panel        │
│ (縮圖列表) │     (主閱讀區)                │ (可收合)        │
│            │                              │                 │
│ [頁面切割  │                              │ [聊天訊息]      │
│  工具面板] │                              │ [輸入框]        │
│            │                              │ [月幣餘額]      │
├────────────┴──────────────────────────────┴─────────────────┤
│ Bottom: 頁面導覽列                                           │
└─────────────────────────────────────────────────────────────┘
```

| 元件 | 說明 |
|------|------|
| 頁面縮圖 | 左側列表，點擊跳轉，當前頁有金色左邊框 |
| PDF Canvas | 中央主區，白色紙張在深色底上 |
| 切割工具 | 左 sidebar 切換面板：範圍選擇、模式切換（range/每N頁/自訂） |
| AI 圖書館員面板 | 右側可收合面板（詳見下方） |
| Zoom 控制 | +/−/fit 按鈕 |
| 頁碼跳轉 | 輸入框 / 上下頁箭頭 |

### 3. AI 圖書館員面板

```
┌─────────────────────────┐
│ [頭像] 圖書館員   [X]   │
│ ● 在線                  │
├─────────────────────────┤
│ [文字] [語音] [人設]    │  ← 模式切換列
├─────────────────────────┤
│                         │
│ 💬 對話區               │
│                         │
│ [AI] 你好，需要什麼幫助？│
│ [我] 這段話什麼意思？    │
│ [AI] ⋯ (打字中動畫)     │
│                         │
├─────────────────────────┤
│ 預估 tokens: ~120       │
│ 消耗: 1 月幣  餘額: 99  │
├─────────────────────────┤
│ [輸入框............] [→] │
└─────────────────────────┘
```

**狀態：**
- 收合態：只顯示觸發按鈕（Topbar 上的圖示）
- 展開態：右側面板 300px 寬
- AI 回覆中：打字動畫（三點跳動）
- 離線態：顯示「需要網路連線」

**AI 頭像設計需求：**
- 需要一個「圖書館員」的角色圖示/頭像
- 風格：與品牌一致的古典感，可以是簡約線條插畫
- 日後可替換為自訂立繪

### 4. Markdown 編輯器（modules/markdown/）

```
┌──────────────────────────────────────────┐
│ Toolbar: [粗體][斜體][標題][列表][連結]... │
├───────────────────┬──────────────────────┤
│                   │                      │
│  編輯區           │   即時預覽           │
│  (等寬字型)       │   (渲染後樣式)       │
│                   │                      │
└───────────────────┴──────────────────────┘
│ Footer: 字數統計 · 匯出選單              │
└──────────────────────────────────────────┘
```

| 元件 | 互動 |
|------|------|
| Toolbar 工具列 | icon buttons, tooltip on hover |
| 編輯區 | 等寬字型、行號、syntax highlight |
| 預覽區 | 模擬文章閱讀樣式 |
| 匯出選單 | MD / HTML / Google Sheets / WordPress |

### 5. 試算表編輯器（modules/spreadsheet/）

```
┌──────────────────────────────────────────┐
│ Toolbar: [字型][大小][粗體][對齊][顏色]   │
├──────────────────────────────────────────┤
│ 公式列: fx [========================]    │
├────┬────┬────┬────┬────┬────┬────┬──── │
│    │ A  │ B  │ C  │ D  │ E  │ F  │... │
├────┼────┼────┼────┼────┼────┼────┼──── │
│ 1  │    │    │    │    │    │    │    │
│ 2  │    │    │    │    │    │    │    │
│... │    │    │    │    │    │    │    │
├──────────────────────────────────────────┤
│ [Sheet1] [Sheet2] [+]     儲存狀態       │
└──────────────────────────────────────────┘
```

| 元件 | 說明 |
|------|------|
| 工具列 | 字型、大小、粗體斜體、對齊、儲存格顏色 |
| 儲存格 | 選中態有金色邊框，編輯態有輸入游標 |
| Sheet 頁籤 | 底部多工作表切換 |
| 右鍵選單 | 插入行/列、刪除、格式化 |

### 6. 書籍排版器（modules/book-editor/）

```
┌────────────────────────────────────────────┐
│ Toolbar: [新增頁] [排序] [匯出PDF] [預覽]  │
├─────────────┬──────────────────────────────┤
│ Pages Panel │                              │
│ (拖曳排序)  │     Page Canvas              │
│             │     (書頁編輯區)              │
│ [封面]      │                              │
│ [第1頁]     │     標題: [________]         │
│ [第2頁]     │     內文: [textarea]         │
│ [+新增]     │     圖片: [上傳/URL]         │
│             │                              │
└─────────────┴──────────────────────────────┘
```

| 元件 | 說明 |
|------|------|
| 頁面清單 | 左側，可拖曳排序，標示封面/內頁 |
| 編輯區 | 標題、內文、圖片位置編輯 |
| 匯出 | PDF / HTML |
| 預覽模式 | 模擬書本翻頁 |

### 7. 公共書庫（public-library/）

```
┌──────────────────────────────────────────┐
│        AKASHA PUBLIC LIBRARY             │
│        公共書庫 — 同樂會                  │
├──────────────────────────────────────────┤
│ [搜尋框................................] │
├──────────────────────────────────────────┤
│ [全部] [文學] [哲學] [歷史] [科學] [童話] │
├──────────────────────────────────────────┤
│ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐   │
│ │ 封面 │ │ 封面 │ │ 封面 │ │ 封面 │   │
│ │      │ │      │ │      │ │      │   │
│ ├──────┤ ├──────┤ ├──────┤ ├──────┤   │
│ │書名  │ │書名  │ │書名  │ │書名  │   │
│ │作者  │ │作者  │ │作者  │ │作者  │   │
│ │頁數·類│ │頁數·類│ │頁數·類│ │頁數·類│   │
│ └──────┘ └──────┘ └──────┘ └──────┘   │
└──────────────────────────────────────────┘
```

| 元件 | 說明 |
|------|------|
| 書封卡片 | 封面圖 + 書名 + 作者 + 頁數 + 分類標籤 |
| 閱讀進度 | 卡片上小標記：「▶ 第 42 頁 (35%)」 |
| 分類篩選 | 膠囊按鈕列，選中態有金色背景 |
| 搜尋 | 搜尋圖示 + input，即時篩選 |
| Empty state | 無結果時的友善提示 |

### 8. AI 設定頁（modules/ai-settings/）

| 元件 | 說明 |
|------|------|
| 模式切換 | BYOK / 月幣 雙按鈕切換 |
| BYOK 面板 | 服務商選擇、API Key 輸入（密碼遮罩）、模型選擇、測試連線按鈕 |
| 月幣面板 | 餘額大數字顯示、費率表、使用紀錄列表 |
| 預估器 | 輸入字數 → 顯示 token 數 → 顯示消耗月幣 |
| 狀態指示 | 綠色「已設定」/ 紅色「尚未設定」 |

### 9. 管理員工具（public-library/admin.html）

| 元件 | 說明 |
|------|------|
| 書目列表 | 書名 + 作者 + 類別 + 編輯/刪除按鈕 |
| 編輯表單 | 書名、作者、分類（下拉）、頁數、描述、Drive ID、封面 URL |
| 匯出區 | 匯出 JSON 按鈕 + 複製按鈕 + 預覽區 |

### 10. 隱私權政策頁（privacy.html）

- 純文字頁面，8 個段落
- 簡潔排版，最大寬度 720px
- 不需特殊元件設計

---

## 共用元件庫

以下元件在多個頁面重複使用，請統一設計：

| 元件 | 變體 |
|------|------|
| Button | default, gold (主要), danger (刪除), disabled |
| Input | text, password, number, textarea |
| Select | 下拉選單 |
| Toast | 成功（綠）、錯誤（紅）、資訊（金）— 右下角浮出 |
| Modal / Confirm | 確認刪除等操作 |
| Empty State | 圖示 + 文字 + 可選行動按鈕 |
| Badge / Tag | 格式標籤（.md .pdf .xlsx）、分類標籤 |
| Toggle | 開關切換 |
| Card | 模組卡片、書籍卡片 |
| Tab / 模式切換 | 膠囊式多選一（如文字/語音/人設） |
| Status Indicator | ● 綠色在線 / ● 紅色離線 / ● 金色同步中 |
| Scrollbar | 自訂窄捲軸（4px 寬，深色） |
| Tooltip | hover 時的浮動說明 |

---

## 圖示需求

目前使用 Unicode emoji 佔位，需要替換為統一風格的 SVG 圖示：

| 用途 | 目前佔位 | 建議風格 |
|------|----------|----------|
| Markdown | ✎ | 鋼筆/羽毛筆 |
| PDF | 📖 | 打開的書 |
| 試算表 | 📊 | 表格/格子 |
| 書籍排版 | 💛 | 書本/裝訂 |
| AI 設定 | ⚙ | 齒輪 |
| 搜尋 | 🔍 | 放大鏡 |
| 公共書庫 | 🌎 | 地球或開放書架 |
| 個人書庫 | 📚 | 書架/書堆 |
| 總覽 | ☰ | 儀表板/網格 |
| AI 圖書館員 | 🤖 | 人形書僮/戴眼鏡的角色 |
| 同步 | — | 雲朵+箭頭 |
| 離線 | — | 斷線雲 |
| 登入 | — | 人形+鑰匙 |
| 匯出 | — | 下載箭頭 |
| 新增 | — | + 號 |
| 刪除 | — | 垃圾桶 |
| 編輯 | — | 鉛筆 |

**圖示風格要求：**
- 線條式（line icon），不填色
- 筆畫粗細統一（1.5-2px）
- 大小可統一為 24×24 viewBox
- 顏色通過 CSS `currentColor` 控制

---

## 動畫與轉場

| 情境 | 建議動畫 |
|------|----------|
| 側欄展開/收合 | slide + fade, 200ms |
| 卡片 hover | translateY(-2px) + shadow 加深 |
| Toast 出現 | 從下方滑入 + fade in |
| AI 打字中 | 三點依序跳動（bounce） |
| 頁面切換 | fade 或 slide-left |
| 按鈕 hover | border 色變 + 微縮放 |
| 同步旋轉 | 雲朵圖示 rotate 360° loop |
| Modal 出現 | scale(0.95→1) + backdrop fade |

---

## 響應式斷點

| 斷點 | 行為 |
|------|------|
| >1200px | 全展開，PDF 三欄（sidebar + canvas + AI panel） |
| 768-1200px | Sidebar 收窄或可收合，AI panel 覆蓋式 |
| <768px | Sidebar 變漢堡選單，單欄佈局，AI panel 全屏覆蓋 |

---

## 設計交付規格

請提供以下交付物：

1. **全域色彩/字型變數表** — 對應 CSS variables
2. **圖示 SVG 套件** — 所有圖示，命名清楚
3. **各頁面 UI 稿** — 桌面版 + 手機版
4. **元件樣式規範** — 每個共用元件的尺寸、間距、圓角、陰影
5. **狀態展示** — hover / active / disabled / error / loading
6. **AI 圖書館員角色設計** — 頭像、表情變化（開心/思考/驚訝）

---

## 技術限制（設計師注意事項）

- 所有樣式只用 CSS 實現（不依賴 JS 動畫庫）
- 圖示用 SVG inline 或 SVG sprite，不用 icon font
- 不要用固定 px 寬高的佈局，保持 flex/grid 彈性
- CSS class 使用 BEM 命名規則：`.block__element--modifier`
- 深色主題是預設，淺色主題是選配切換
- 字型大小範圍：10px（最小輔助）~ 28px（品牌標題），主文 13-14px
- 最大內容寬度：1000px（公共書庫）/ 無限制（編輯器類）

---

## 範例 Class 命名對照

```css
/* 已有的 class 結構，設計稿可依此命名 */
.sidebar              /* 側欄整體 */
.sidebar-brand        /* 品牌區 */
.sidebar-auth         /* 登入區 */
.sidebar-footer       /* 底部 */
.nav-item             /* 導覽項 */
.nav-item.active      /* 當前項 */

.module-card          /* 儀表板卡片 */
.module-card:hover    /* 卡片hover */
.card-icon            /* 卡片圖示 */
.card-title           /* 卡片標題 */

.book-card            /* 書籍卡片 */
.book-card__cover     /* 封面區 */
.book-card__title     /* 書名 */
.book-card__category  /* 分類標籤 */

.ai-panel             /* AI 面板 */
.ai-panel--hidden     /* AI 面板收合 */
.ai-panel__header     /* AI 面板頂部 */
.ai-panel__messages   /* 對話區 */
.ai-msg               /* 單則訊息 */
.ai-msg--user         /* 用戶訊息 */
.ai-msg--assistant    /* AI 訊息 */

.filter-btn           /* 篩選按鈕 */
.filter-btn--active   /* 選中篩選 */

.btn                  /* 通用按鈕 */
.btn-gold             /* 主要按鈕 */
.btn-danger           /* 危險按鈕 */

.toast                /* 提示條 */
.toast.visible        /* 顯示中 */
```

---

## 額外設計機會（非必要但歡迎）

- 啟動畫面 / Splash screen（PWA 開啟動畫）
- 404 頁面
- 深色↔淺色模式切換動畫
- 書籍閱讀時的翻頁效果
- AI 圖書館員的多種表情/狀態立繪
- 公共書庫的書架視覺化（像真實書架那樣排列書本）
- 成就/徽章系統 UI（未來功能）
